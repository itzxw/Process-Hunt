# Process-Hunt
Process Hunt is a C-based memory enumeration tool designed to analyze running Windows processes. It utilizes the Toolhelp32 API to find specific targets, maps their loaded DLLs, and uses ReadProcessMemory to extract their Export Directory Tables dynamically into a report
